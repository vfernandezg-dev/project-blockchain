{
  "name": "vitalpaws-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Contratos Solidity de VitalPaws — Cases + ImpactNFT (Hardhat, Sepolia).",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.17"
  }
}
